@model IEnumerable<QUANLYNHAHANG.Models.Loaisanpham>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tên_loại_SP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tên_loại_SP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Mã_loại_SP }) |
            @Html.ActionLink("Details", "Details", new { id=item.Mã_loại_SP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Mã_loại_SP })
        </td>
    </tr>
}

</table>
